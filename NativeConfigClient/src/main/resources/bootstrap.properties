# 二者拼接寻找 Config
spring.application.name=configclient
spring.profiles.active=dev
# 本地 Config Server 访问路径
spring.cloud.config.uri=http://localhost:8762
# 设置客户端优先判断 Config Server 获取是否正确
spring.cloud.config.fail-fast=true